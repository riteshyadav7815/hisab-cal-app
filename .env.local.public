# Public environment variables for OAuth providers
# These are exposed to the client-side and used to conditionally render social login buttons

# Google OAuth (set to your Google Client ID when configured)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=

# Facebook OAuth (set to your Facebook Client ID when configured)
NEXT_PUBLIC_FACEBOOK_CLIENT_ID=